# Group Expense Splitter

Split a shared expense that was paid by one person and share the state via the URL.

## Features
- Payer name, total amount, and people list stored in the query string
- URL restores the split on load
- Duplicate names merged in the split

## Development
```bash
npm install
npm run dev
```
